[EP-001][M1] confirm locked npm toolchain and fix the declared unit command

Inventory executed and captured at .agent/evidence/EP-001/M1-inventory.txt:
lockfile TRACKED, sha256 06e7cb243f81a393c5b7b5a6901338b864af83c9f5fbbeb1f5cd6b266af5d80a,
node v24.14.1 / npm 11.11.0 / psql 16.14 / Python 3.14.4 / Docker 29.7.2.

package.json now delegates every npm entry point to its shell gate so there is one
implementation of each check, pins packageManager to the observed npm 11.11.0, and
keeps the dependency set exactly as before (typescript 5.9.3, @types/node 24.10.1).

Departure from the ExecPlan, recorded in COMMANDS.md: the plan prescribed test:unit
as `node --test "tests/**/*.test.ts"`. That glob sweeps in the integration, db,
blackbox, api, e2e, live-fire and release suites, which need a provisioned database,
a running API or a built artifact - so the entry point would fail on a clean checkout
and present a harness limitation as a product failure (DOD-032). test:unit delegates
to scripts/test-unit.sh, which excludes those roots per the binding suite table in
TESTING.md. Coverage of the 94 pure tests is unchanged.

Observed: test-unit: ok (94 pass, 0 fail); test collection guard: ok
({"tests":94,"fail":0,"verdict":"OK"}).
