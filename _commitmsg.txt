[FORGE] author the missing specification half; implement EP-000 discovery and toolchain

The pack had a governance control plane and no product specification: all 9 SPEC
files were 155-byte stubs, all 11 ExecPlans were 10-line stubs with no milestones,
CLAIM_TO_RELEASE_TRACEABILITY.csv had zero rows, and there was no source code.

SPECIFICATIONS (SPEC-000 .. SPEC-008), authored as the oracle:
- SPEC-000  vocabulary lock, the eleven product truth states with non-collapse
            rules, VG-* requirement catalogue with acceptance oracles and required
            negative cases, channel priority, coverage-honesty rules
- SPEC-001  entities, value objects, the closed T1-T21 transition table, the six
            explicitly forbidden pairs, domain ports, commands and events
- SPEC-002  schema, RLS (enabled + forced, fail-closed on missing tenant), envelope
            encryption, retention, migration and restore rules
- SPEC-003  Fastify route catalogue, idempotency, webhook ingress, MCP surface
- SPEC-004  surfaces, the truth-state presentation contract, coverage honesty,
            WCAG 2.2 AA, anti-dark-pattern rules
- SPEC-005  OIDC/MFA, roles, AuthorityGrant and anti-fraudulent-enrollment controls
- SPEC-006  outcome vs candidate-failure vs system-error taxonomy, fail-closed and
            no-masking rules
- SPEC-007  signals, DLP-safe telemetry, metric catalogue, alerting, SLOs
- SPEC-008  ship standard, candidate epochs, 484-ID accounting, the four verdicts

EXECPLANS authored to the Section 11 grammar (14 sections, 9 milestone keywords,
unchecked progress): EP-000, 001, 004, 005, 006, 007, 008, 009, 010.
EP-002 and EP-003 remain stubs and are recorded as such.

DOMAIN CORE, real and tested: value objects, typed error taxonomy, the eleven truth
states with non-collapse facts encoded as data, the closed transition table with
guard evaluation and refusal semantics. 94 tests execute and pass via node:test
against the TypeScript sources; the domain layer imports only the standard library,
enforced by scripts/import-boundary.sh.

EP-000 MILESTONES IMPLEMENTED (real gates replacing hollow placeholders):
- gates with a real underlying implementation now run it; the rest still fail
  loudly with the mandated ERROR signature
- scripts/test-collection-guard.sh fails on zero collected tests, on an emptied
  test file, on an emptied manifest suite, and on any failing test (all four proven
  by negative tests, not asserted)
- scripts/gate-toolchain.sh mechanically rejects any script that prints a success
  sentinel without performing a check

DEFECTS FOUND AND FIXED IN THE PACK'S OWN TOOLING:
- .gitignore excluded .agent/evidence/, contradicting DOD-025 and hiding evidence
  from validate-generated-pack.py on a fresh clone
- validate-generated-pack.py had any(part not in SKIP_DIRS ...), so node_modules
  was scanned and its contents reported as pack defects
- validate-generated-pack.py treated balanced JSON braces as template-placeholder
  residue; it now matches unresolved tokens and was proven to still catch a seeded
  one

NOT CLAIMED: the product does not exist. No API, UI, persistence, auth,
observability, artifact or deployment. verify.sh stops at format-check, which is
the correct honest state. RELEASE_GATE.json remains INCONCLUSIVE. DOD_STATUS
records 5 PASS, 12 PARTIAL, 24 NOT_STARTED, 1 EXTERNAL_REQUIRED out of 42 clauses.
The 484-ID accounting has not begun.
