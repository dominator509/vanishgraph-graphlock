/**
 * PostCSS configuration (EP-005 M1).
 *
 * IT IS EMPTY ON PURPOSE, and the plan lists the file because an absent one is ambiguous: a reader cannot tell whether
 * no CSS pipeline was chosen or one was simply forgotten. No plugin is configured, so the CSS a page imports is what
 * the browser receives — which is the honest starting point for a milestone whose job is the route tree and the gate,
 * not the visual system.
 */
export default { plugins: {} };
