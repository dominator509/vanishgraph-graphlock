# Commands

Run from repo root with CI=true GIT_TERMINAL_PROMPT=0 GIT_PAGER=cat PAGER=cat DEBIAN_FRONTEND=noninteractive. Commands: `sh scripts/preflight.sh`; `sh scripts/graph-next.sh`; `sh scripts/ledger.sh tail 30`; `python3 scripts/validate-generated-pack.py .`; `python3 scripts/anti-gaming-scan.py .`; `sh scripts/verify.sh`; `sh scripts/production-readiness-check.sh`. Coding agents must not invent commands.
