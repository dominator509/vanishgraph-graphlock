provisioning attempts - 2026-09-17T16:49:38Z

== job-worker ==
attempt: docker ps -a --filter name=worker  (is any worker process running to write a heartbeat?)
attempt: git ls-files 'src/**' | grep -i worker  (does the repository contain a worker entry point at all?)
verdict: CODE - NO WORKER ENTRY POINT EXISTS IN THIS REPOSITORY, so nothing can write the heartbeat this probe
  reads. Provisioning cannot fix it; a worker node must exist first.

== object-store ==
attempt: docker images minio/minio  (is an object store available to run locally?)
  minio/minio:RELEASE.2025-09-07T16-13-09Z-cpuv1
  minio/minio:latest
  minio/minio:RELEASE.2025-07-23T15-54-02Z
  minio/minio:RELEASE.2025-04-22T22-12-26Z
  minio/minio:RELEASE.2024-12-13T22-19-12Z
verdict: CODE - AN OBJECT STORE CAN BE STARTED FROM THE LOCAL IMAGE, SO PROVISIONING IS NOT THE BLOCKER: the
  declared action is HeadBucket plus a SIGNED GetObject and no module in this repository can sign an S3 request,
  so the probe would still not run with a store available.

== keycloak-jwks ==
attempt: KEYCLOAK_ISSUER preset check (the declared discovery issuer)
  unset
attempt: docker images --filter reference='*keycloak*'
verdict: ENVIRONMENT - no issuer is configured and no Keycloak image is cached locally, so discovery cannot be
  reached. The probe itself is implemented and would run against a real issuer.

== provider-transport ==
attempt: provider entitlement probes (scripts/probes/*.sh) - is any official transport credentialed?
  scripts/probes/postal_api.sh: no credential
  scripts/probes/search_api_key.sh: no credential
  scripts/probes/stripe.sh: no credential
verdict: EXTERNAL_REQUIRED - no provider entitlement exists in this environment, and a provider run is recorded
  EXTERNAL_REQUIRED everywhere else in this repository for the same reason.

