^__6L_ALLOW_NONE__$

# Reviewed allow-list for the reality gate (DOD-019).
#
# Format: <path>:<line> <reason>
# A hit is permitted only by an entry here. Entries are per-line and reviewed; a blanket or
# wildcard entry is prohibited, because the whole point of the gate is that every exception
# is a decision someone made on purpose.
#
# EMPTY BY DESIGN (the sentinel line above is a placeholder meaning "no entries").
#
# The three entries added in [EP-003][M1] were removed in [EP-003][M2] when the rule itself was
# corrected. Those hits were false positives: src/infrastructure/database/*.ts are pinned CLI
# helpers whose contract is to print an exact sentinel that a wrapper script greps for, and the
# rule now excludes that directory by scope rather than excusing individual lines. Preferring a
# precise rule to a growing allow-list is the point: an allow-list that gains entries every
# node stops being a review and becomes a formality.
#
# This file stays empty until a hit is genuinely simulated, unfinished, or fabricated AND is
# accepted as a known, time-bounded exception. If you are adding an entry, the burden is on the
# reason string to say why the behaviour is correct rather than why the gate is inconvenient.
