^__6L_ALLOW_NONE__$

# Reviewed allow-list for the reality gate (DOD-019).
#
# Format: <path>:<line> <reason>
# A hit is permitted only by an entry here. Entries are per-line and reviewed; a blanket or
# wildcard entry is prohibited, because the whole point of the gate is that every exception
# is a decision someone made on purpose.
#
# To add an entry: run `sh scripts/reality-gate.sh`, read the named file and line, and satisfy
# yourself that the match is not simulated, unfinished, or fabricated behaviour.

src/infrastructure/database/provision.ts:340 harness sentinel: this pinned CLI helper's contract is to print 'db provision: ok' on success; the wrapper script requires it
src/infrastructure/database/provision.ts:369 harness sentinel: teardown prints 'db teardown: ok' only after proving the container is absent
src/infrastructure/database/provision.ts:404 harness sentinel: the no-state-file teardown path prints the same sentinel for an idempotent no-op, which is a real outcome, not a skip
